---
- hosts: all
  become: yes
  tasks:
   - name: encrypt and set password
     command: 
         useradd {{ my_username }}
   - name: encrypt
     command:
         echo "{{ my_password }}" | passwd "{{ my_username }}"
         
  
    
